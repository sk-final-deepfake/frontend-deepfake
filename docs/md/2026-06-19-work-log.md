# 2026-06-19 작업 일지 (ForenShield / E2E 파이프라인)

> 오늘 채팅·작업 기준 정리.  
> 목표: **GPU Xception 추론 → 백엔드 저장 → 프론트 사건 상세 표시** E2E 완성 및 `develop` 배포 안정화.

---

## 1. 오늘 한 일 요약

| 구분 | 내용 | 상태 |
|------|------|------|
| GPU Worker | evidence 59·60 Xception 추론 성공, RabbitMQ 결과 발행 | ✅ |
| 프론트 develop | 팀원 리팩토링 + 로컬 Xception UI 병합 | ✅ push |
| 프론트 컨벤션 | `common_rule.md` 기준 위반 수정 | ✅ push |
| 프론트 버그 | 한글 사건명 `caseKey` 이중 인코딩 → 사건 상세 404 | ✅ fix + push |
| 팀 develop 동기화 | PR #17 (`feature/refresh2`) rebase 후 push | ✅ `c21e1ee` |

---

## 2. 배경: E2E 파이프라인

```
영상 업로드 → S3 → RabbitMQ → GPU Worker (Xception) → 백엔드 결과 저장 → 프론트 사건 상세
```

### 주요 설정 (GPU)

| 항목 | 값 |
|------|-----|
| SSH | `sk4team@58.127.241.84` |
| Worker 경로 | `~/forenShield-ai` |
| `INFERENCE_MODE` | `local_model` |
| `INFERENCE_DEVICE` | `cuda` |
| 체크포인트 | `models/test/video/xception/v1.0.0/xception_best.pth` |
| RabbitMQ | `10.0.11.53:31624` |

### riskScore 계산 (GPU Worker)

`ai/ai-forensic/gpu_worker/inference_runner.py` → `_risk_from_scores()`:

```python
weights = {
  "deepfake": 0.45,
  "frameEdit": 0.25,
  "lipSync": 0.15,
  "reEncoding": 0.10,
  "splicing": 0.05,
}
risk = sum(scores[k] * weights[k] for k in weights) * 100.0
```

- deepfake만 0.9416이면 riskScore ≈ **42.4 (MEDIUM)**
- 백엔드는 GPU가 보낸 `riskScore`를 그대로 DB에 저장

---

## 3. GPU Worker — 오늘 확인된 성공 사례

### evidence 59 (`youtube_short_r1fQJk69ryM.mp4`)

```
analysisRequestId=39
Downloaded → Xception inference (32 frames, deepfake=0.9416, ~1.01s)
Published result status=COMPLETED → ai.result.exchange/result.video
```

### evidence 60 (`local-upload-test.mp4`, 사건명 `ㅇㅇㅇ`)

```
analysisRequestId=40
S3: cases/ㅇㅇㅇ/60/copy/local-upload-test.mp4
Xception inference (3 frames, deepfake=0.9873, ~0.25s)
Published result status=COMPLETED
```

**결론:** GPU·큐·S3 다운로드·추론·결과 발행은 정상. 문제는 **프론트 사건 상세 진입** 쪽이었음.

---

## 4. 프론트 develop 작업 타임라인

### 4-1. `git pull` 실패 (로컬 수정 vs 원격 리팩토링)

**증상**

```text
error: Your local changes to the following files would be overwritten by merge:
  app/evidences/[id]/page.tsx
  app/main/_components/analysis-request-flow.tsx
```

**원인**

- 로컬: Xception 상세 UI 수정 (미커밋)
- 원격 `develop`: 상세 페이지 대규모 리팩토링 (4커밋, `evidences/[id]` → `/cases/{caseId}` 리다이렉트 구조)

**해결**

1. 로컬 변경 `git stash`
2. `git pull origin develop` (fast-forward)
3. stash 내용을 **새 구조**에 맞게 이식 (아래 4-2)

---

### 4-2. Xception UI를 새 사건 상세 구조에 반영

**커밋:** `e36b20f` — `feat: Xception GPU 분석 결과를 사건 상세 UI에 반영`

원격 리팩토링으로 예전 파일 역할이 바뀜:

| 예전 | 이후 |
|------|------|
| `app/evidences/[id]/page.tsx` (풀 상세) | 증거 ID → 사건 상세로 **리다이렉트**만 |
| `components/analysis-result.tsx` | 삭제, 탭 컴포넌트로 분리 |
| 상세 UI | `app/cases/[id]/_components/*` |

**추가·수정 파일**

| 파일 | 역할 |
|------|------|
| `lib/analysis-display.ts` | Xception 모델명, `moduleResults` JSON 파싱·표시 문구 |
| `app/cases/[id]/page.tsx` | API `summary`를 위험 라벨로 표시 |
| `app/cases/[id]/_components/deepfake-model-tab.tsx` | 모델명 `Xception (test)`, 점수 상세 포맷 |

---

### 4-3. `common_rule.md` 컨벤션 점검 및 수정

**커밋:** `213652c` — `refactor: common_rule 컨벤션에 맞게 사건 상세·분석 표시 정리`

**문서:** `docs/md/common_rule.md` (repo에 포함)

| 규칙 | 기존 문제 | 조치 |
|------|-----------|------|
| §4 real 모드 가짜 결과 금지 | summary-tab에 "정상 영상", "정상", "통과" 하드코딩 | API/상태 기반 문구 (`분석 대기`, `분석 근거 없음`) |
| §4 | metadata-report Recovery Score 98/64 임의 수치 | "백엔드 제공 시 표시" 안내 UI |
| §4 | 영상 길이/FPS 기본값(31.24초 등) | 없으면 `확인되지 않음` |
| §7 | `formatDateTimeWithSeconds` 중복 | `@/lib/formatters` 사용 |
| §7 | 위험도/상태 라벨 화면별 중복 | `@/lib/status-labels` + `_lib/evidence-display.ts` |
| §5 | `useMemo(buildProgressSteps)` | 렌더 시 직접 계산 |
| §6 | `page.tsx` 비대 | `app/cases/[id]/_lib/evidence-display.ts` 분리 |

**추가 파일**

- `app/cases/[id]/_lib/evidence-display.ts` — `getDisplayRiskLabel`, `getModuleSummaryValues`, `buildProgressSteps` 등

---

### 4-4. 팀원 develop 병합 (PR #17)

**커밋 (팀원)**

- `484f108` — 로그인 성공 후 뒤로가기 시 로그인 화면 재노출 방지
- `0f8e046` — Merge PR #17 `feature/refresh2`

**로컬 처리**

```bash
git fetch origin develop
git pull --rebase origin develop   # caseKey fix를 팀 커밋 위에 rebase
npm run build                      # 통과
git push origin develop            # c21e1ee
```

---

## 5. 트러블슈팅: 분석 완료 후 사건 상세 404

### 5-1. 증상

- URL: `https://forensheildjangdochi.com/cases/유튜브%20쇼츠6?evidenceId=59`
- 화면: **「데이터 로드 오류 — 현재 DB에서 이 사건을 찾을 수 없습니다」**
- GPU 로그: evidence 59 분석 **COMPLETED** (추론 성공)
- 브라우저 콘솔:

```text
GET /api/v1/cases?caseKey=%25EC%259C%25A0%25ED%258A%259C%25EB%25B8%258C%2520%25EC%2587%25BC%25EC%25B8%25A06
→ 404
```

(Vercel Analytics 스크립트 404는 별도 이슈, 기능과 무관)

### 5-2. 원인 분석

**핵심: `caseKey` 이중 URL 인코딩**

1. 분석 완료 후 이동 URL 생성 시 `encodeURIComponent(사건명)` → 경로에 1회 인코딩  
   예: `/cases/%EC%9C%A0%ED%8A%9C%EB%B8%8C%20%EC%87%BC%EC%B8%A06`
2. Next.js `useParams().id`가 **이미 인코딩된 문자열**을 그대로 반환하는 경우 있음  
   → `caseId` = `%EC%9C%A0%ED%8A%9C%EB%B8%8C%20%EC%87%BC%EC%B8%A06`
3. `fetchCaseDetail`에서 다시 `encodeURIComponent(caseId)` → **2중 인코딩**  
   → `%25EC%25...` (`%`가 `%25`로 또 인코딩)
4. 백엔드는 `caseKey`로 DB 조회:

```sql
-- EvidenceRepository
AND (e.caseNumber = :caseKey OR e.caseName = :caseKey)
```

- 기대값: `유튜브 쇼츠6`
- 실제 전달: `%EC%9C%A0%ED%8A%9C%EB%B8%8C%20%EC%87%BC%EC%B8%A06` (문자 그대로)
- → **CASE_NOT_FOUND (404)**

**왜 GPU는 됐는데 화면만 안 됐나**

- 분석·저장은 `evidenceId` 기준으로 정상
- 사건 상세는 **URL의 사건명(caseKey)** 으로 조회 → 인코딩 버그만 터짐
- 한글·공백 사건명에서 특히 재현

### 5-3. 해결

**커밋:** `c21e1ee` — `fix: 한글 사건명 caseKey 이중 인코딩으로 상세 404 나는 문제 수정`

#### (1) 공통 유틸 추가 — `lib/route-params.ts`

```ts
decodeRouteParam(value)      // 1~3회 decodeURIComponent로 이중 인코딩 해소
buildCaseDetailPath(caseKey, evidenceId?)  // decode 후 1회만 encode
```

#### (2) API 호출 정규화 — `lib/api/evidence-detail.ts`

```ts
const caseKey = decodeRouteParam(caseId)
const params = new URLSearchParams({ caseKey })
return apiRequest(`/api/v1/cases?${params}`)
```

#### (3) 사건 상세 페이지 — `app/cases/[id]/page.tsx`

```ts
const caseId = decodeRouteParam(Array.isArray(id) ? id[0] : id)
```

#### (4) 분석 완료 후 이동 경로 변경 — `analysis-request-flow.tsx`

사건명 URL 직행 대신 **증거 ID 경유** (인코딩 이슈 회피):

```ts
// 변경 전
router.push(`/cases/${encodeURIComponent(caseName)}?evidenceId=...`)

// 변경 후
router.push(`/evidences/${evidenceId}`)
```

`/evidences/[id]` → `fetchEvidenceDetail` → API의 `caseName`으로 `/cases/...` 리다이렉트 (`buildCaseDetailPath` 사용)

#### (5) 링크 일괄 정리

- `app/evidences/[id]/page.tsx`
- `components/recent-analyses.tsx`
- `app/main/_components/dashboard-overview.tsx`
- `app/mypage/_components/case-history-list.tsx`

### 5-4. 검증 방법 (배포 후)

1. 사건명 `유튜브 쇼츠6` 등 **한글+공백**으로 업로드·분석 요청
2. 분석 완료 후 `/evidences/{id}` → `/cases/...?evidenceId=...` 자동 이동
3. DevTools Network에서 `GET /api/v1/cases?caseKey=` 확인  
   - `caseKey`가 **한 번만** 인코딩 (`%EC%9C%...`, `%25` 없음)
4. 사건 상세 탭에서 Xception summary·moduleResults 표시 확인

---

## 6. 이전 세션에서 이어진 GPU 이슈 (참고)

오늘 채팅 맥락에 포함된 선행 트러블슈팅:

| 증상 | 원인 | 조치 |
|------|------|------|
| `set: pipefail` invalid option | Windows CRLF | `sed -i 's/\r$//'` / LF 변환 |
| timm dataclass 에러 | timm 0.6.12 + Python 3.12 | `timm>=1.0.8` |
| deepfakeScore ~0.5 고정 | 체크포인트 키·구조 불일치 | `xception_ffpp_net.py` (`adjust_channel` 512, `last_linear` 2048→2) |
| AV1 `.f399.mp4` 실패 | OpenCV AV1 미지원 | H.264 mp4 또는 `video_normalize.py` ffmpeg fallback |
| AMQPConnectionError | `.env` localhost / VPN | `RABBITMQ_HOST=10.0.11.53:31624` |

---

## 7. 오늘 프론트 develop 커밋 목록 (본인·팀 포함)

```text
c21e1ee fix: 한글 사건명 caseKey 이중 인코딩으로 상세 404 나는 문제 수정  ← 오늘 최종
0f8e046 Merge pull request #17 (feature/refresh2)                        ← 팀원
484f108 fix: 로그인 성공 후 뒤로가기 로그인 화면 재노출 방지              ← 팀원
213652c refactor: common_rule 컨벤션에 맞게 사건 상세·분석 표시 정리
e36b20f feat: Xception GPU 분석 결과를 사건 상세 UI에 반영
a1e0a96 루트 레이아웃 인증 provider 중복 import 제거                    ← 팀원 리팩토링
8d64901 비교 검증 썸네일과 상세 페이지 흐름 정리                          ← 팀원 리팩토링
```

---

## 8. 변경 파일 맵 (오늘 직접 수정·추가)

```
frontend-deepfake/
├── docs/md/
│   ├── common_rule.md                          # 컨벤션 문서 (신규)
│   └── 2026-06-19-work-log.md                  # 본 문서
├── lib/
│   ├── analysis-display.ts                     # Xception 표시 헬퍼
│   └── route-params.ts                         # caseKey 인코딩 fix (신규)
├── app/cases/[id]/
│   ├── page.tsx
│   ├── _lib/evidence-display.ts                # 사건 상세 표시 로직 (신규)
│   └── _components/
│       ├── deepfake-model-tab.tsx
│       ├── summary-tab.tsx
│       └── metadata-report-tab.tsx
├── app/evidences/[id]/page.tsx
├── app/main/_components/
│   ├── analysis-request-flow.tsx
│   └── dashboard-overview.tsx
├── app/mypage/_components/case-history-list.tsx
└── components/
    ├── recent-analyses.tsx
    └── analysis-progress.tsx
```

---

## 9. 아직 남은 / 후속 확인

| 항목 | 설명 |
|------|------|
| 배포 반영 | `develop` → 프로덕션 배포 후 evidence 59·60 UI 재확인 |
| 백엔드 `EvidenceInfoDto.caseId` | 프론트 타입에 `caseId` 있으나 API 미포함 → 현재 `caseName` fallback 사용 |
| GPU Worker 코드 Git | `xception_ffpp_net.py` 등 GPU 서버 수동 배포분과 repo 동기화 권장 |
| 백엔드 `clob→text` | 로컬만 수정된 경우 CI 배포 여부 확인 |

---

## 10. 유용한 명령 (GPU 서버)

```bash
# 오프라인 추론 (Worker 재시작 불필요)
python scripts/infer/run_xception_infer.py work/59_youtube_short_r1fQJk69ryM.mp4

# Worker 재시작
pkill -f gpu_worker.rabbitmq_worker
nohup python -m gpu_worker.rabbitmq_worker >> logs/worker.log 2>&1 &

# 결과·로그 확인
cat results/analysis_39_59.json
grep -E "Processing|Xception|Published" logs/worker.log | tail -10
```

---

## 11. 한 줄 결론

**6월 19일:** GPU Xception E2E는 evidence 59·60에서 성공했고, 프론트는 팀 리팩토링과 병합한 뒤 Xception UI·컨벤션을 반영했으며, **한글 사건명 `caseKey` 이중 인코딩**이 분석 후 상세 404의 원인이었고 `decodeRouteParam` + `/evidences/{id}` 경유로 해결해 `develop`(`c21e1ee`)에 push 완료.
