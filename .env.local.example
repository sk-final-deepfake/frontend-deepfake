# Next.js dev server가 백엔드 API로 프록시할 주소 (next.config rewrites)
BACKEND_API_ORIGIN=http://localhost:8080

# develop 쪽 API 클라이언트 (lib/api-config.ts, login/signup 등)
NEXT_PUBLIC_API_URL=http://localhost:8080

# feature/mypage 쪽 API 클라이언트 (lib/api/config.ts)
# 비워두면 같은 출처(/api/v1) + rewrites 사용. 직접 백엔드 호출 시 아래 주석 해제
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
